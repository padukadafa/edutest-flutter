import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF3461FD);
  static const secondary = Color(0xFFFF9F1C);
  static const background = Color(0xFFF8F9FF);
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF6C6C6C);

  static const border = Color(0xFFE0E0E0);
  static const success = Color(0xFF28A745);
  static const error = Color(0xFFDC3545);
  static const warning = Color(0xFFFFC107);
  static const info = Color(0xFF17A2B8);

  static const grey = Color(0xFF808080);
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  static const darkGray = Color(0xFF4A4A4A);
  static const lightGray = Color(0xFFB0B0B0);
  static const transparent = Colors.transparent;
  static const overlay = Color(0x80000000); // 50% opacity black
  static const shadow = Color(0x1A000000); // 10% opacity black
  static const highlight = Color(0xFFFFF3CD); // Light yellow
  static const disabled = Color(0xFFCCCCCC);
  static const link = Color(0xFF0066CC);
  static const hover = Color(0xFFE6F0FF); // Light blue
  static const focus = Color(0xFF99CCFF); // Medium blue
  static const active = Color(0xFF0B2C5D); // Dark blue
  static const visited = Color(0xFF551A8B); // Purple
  static const highlightText = Color(0xFFFFEB3B); // Bright yellow
  static const cardBackground = Color(0xFFFFFFFF);
}
